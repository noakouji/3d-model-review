---
name: 3d-model-review
description: 3Dモデルをコードで作り、ブラウザ上の注釈でレビューして直すループを回すスキル。使用タイミング：(1)「3Dモデル作って」「STL作って」「3Dプリントしたい」と言われた時、(2)「ビューア開いて」「モデル見せて」と言われた時、(3)「注釈読んで」と言われた時、(4) 嵌合部品・筐体パーツ・治具など寸法精度が要るパーツを設計する時。
---

# 3D Model Review

3Dモデルを **コードで作り → ブラウザで注釈させ → 注釈の座標を読んで直す** ループを回す。

やり取りの主導権はモデルではなく **注釈JSON** にある。人間が「ここ」と刺した座標が
そのまま返ってくるので、言葉でのすれ違いが起きない。

---

## 前提

- モデリングは **build123d** で行う。作法は `references/build123d-workflow.md` を先に読む
- ビューアは STL を表示するだけなので、他のCADで作った STL でも同じように使える
- Python 3.10 以上。ビューア自体に追加インストールは要らない

---

## ループの回し方

### 1. 案件ディレクトリを決める

ワークスペースは**作業ディレクトリのパスから決まる**。案件を混ぜないため、
先に案件のディレクトリを切ってから始める。

```bash
mkdir -p ~/work/bracket && cd ~/work/bracket
```

### 2. モデルを作る

`references/build123d-workflow.md` に従って build123d で組む。
1フィーチャーごとに測って進める。**見る前に測る。**

### 3. STL をワークスペースに出す

```bash
python3 -c "
import sys; sys.path.insert(0, 'PATH_TO_THIS_SKILL/viewer')
import workspace
print(workspace.resolve_workspace('$PWD').models_dir)
"
```

出力されたディレクトリに STL をエクスポートする。ファイル名にバージョンを入れる
（`bracket_v1.stl` → `bracket_v2.stl`）。ビューアは更新時刻の新しい順に並べるので、
**過去バージョンと並べて比較できる**。上書きしない。

### 4. ビューアを起動する

**モデルを1つでも出したら、聞かずに起動する。** 「見せますか？」と確認しない。
静止画のレンダリングだけでは、相手が自分で回して確かめられない。

```bash
bash PATH_TO_THIS_SKILL/viewer/start.sh "$PWD" &
```

- ポートは 8765 から自動探索し、実ポートは `~/.model-review/port` に書かれる。**固定しない**
- 起動スクリプトがブラウザまで開く
- 他のプロセスは kill しない

自分でも表示を確認すること。ブラウザ操作ツールが使えるなら別タブで開いて、
モデル一覧・WebGL・現在の選択モデルを検証する。**相手のタブのDOMを覗きに行かない。**

### 5. 注釈を読む

「注釈読んで」と言われたら、相手のブラウザではなくファイルを読む。

```
~/.model-review/workspaces/<slug>/annotations.json
```

slug がわからなければ `viewer/workspace.py` の `slug_for()` で作業ディレクトリから引く。

注釈の色は意図を表す。**コメントより色を優先して解釈する。**

| 色 | 意味 |
|---|---|
| 🔴 `#dc2626` | 削る |
| 🟢 `#22c55e` | 追加 |
| 🔵 `#3b82f6` | 測定 |
| 🟡 `#f59e0b` | 注意 |
| 🟣 `#a855f7` | 相談 |

`coords` は**モデル座標系そのまま**（mm）。`normal` はその点の面法線なので、
どの面を指しているかは法線から判断する。座標だけで面を推測しない。

### 6. 直して 3 に戻る

パラメータを変えて再エクスポート。ビューアはリロードすれば新しいSTLを拾う。
**再起動は要らない。**

---

## 注釈を読むときの判断

- **注釈が1件でも「相談」色なら、直す前に質問する。** 紫は「どうする？」であって
  「直して」ではない
- **座標が面の上にあるとは限らない。** ピックは表示メッシュ上の点なので、
  丸めた角の途中を指していることがある。`normal` と近傍の寸法から意図を復元する
- **「もっと」「ちょっと」は数値に翻訳して確認する。** 「もっと斜めに」を勝手に
  15°と解釈しない。「現状10°を20°にしますか」と数字で返す
- 履歴は `annotations_history/` に溜まる。同じ箇所を繰り返し指摘されているなら、
  部分的な手直しではなく**設計そのものを見直す**

---

## やらないこと

- 相手のブラウザのDOMを読んで注釈を取ろうとする（ファイルを読む）
- STL をリポジトリにコミットする（ワークスペース側に置く）
- ポートを固定する / 既存プロセスを kill する
- 「見せますか？」と確認してからビューアを起動する（先に起動する）
